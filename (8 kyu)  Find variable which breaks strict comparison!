In JavaScript, there is a special case where strict comparison of the same variable returns false! Try to find out what must be done to get such result!
var x = something;
x === x // returns false!
Write a function which will return value for which strict comparison will give false!

В JavaScript существует особый случай, когда строгое сравнение одной и той же переменной возвращает false! Постарайтесь выяснить, что нужно сделать, чтобы получить такой результат!
var x = что-то;
x = = = x//возвращает значение false!
Запишите функцию, которая вернет значение, для которого строгое сравнение даст значение false!

function findStrangeValue() {
   return "circle" / 2;
}
