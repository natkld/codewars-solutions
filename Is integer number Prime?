Find Prime number between integer numbers.

CODE 1:
function isPrime(n) { 
 if (n < 2) return false;
for (let i = 2; i <= n; i++) {
  if (n === 2 || n === 3 || Math.pow(n, 2) % 24 === 1) return true;
  else return false;
}
}
